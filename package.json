
{
  "name": "luckyspin-maple-edition",
  "version": "1.0.0",
  "private": true,
  "description": "A static ESM-based lottery wheel application.",
  "scripts": {
    "build": "echo 'Static build - no action needed'",
    "install": "echo 'No dependencies to install'"
  },
  "dependencies": {},
  "devDependencies": {}
}
